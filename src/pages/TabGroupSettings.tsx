import React from 'react';
import { Box, Typography } from '@mui/material';

const TabGroupSettingsPage: React.FC = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4" gutterBottom>
        Tab Group Settings
      </Typography>
      <Typography variant="body1">
        Settings for tab groups will be available here in the future.
      </Typography>
    </Box>
  );
};

export default TabGroupSettingsPage;
